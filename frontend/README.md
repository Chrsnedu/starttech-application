# Frontend Delivery Assets

The React source remains in `Client/` so the current Vite project structure does not break.

This directory is reserved for deployment-facing frontend artifacts such as environment examples and future CDN-specific helpers.
